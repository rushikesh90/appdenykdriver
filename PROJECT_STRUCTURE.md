# Project Structure

- `agent/` C++ userspace agent
- `kernel/` traditional LSM module
- `bpf/` eBPF LSM implementation
- `policy/` backend-independent policies + schema
- `scripts/` build/load/test helpers
- `cmake/` CMake helper modules
- `docs/` design docs
- `tests/` unit + integration tests
